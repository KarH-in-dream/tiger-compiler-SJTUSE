致读者：
随着Lab的进行，我发现我之前的代码还有诸多不完美之处。
这些潜在的BUG不影响Lab，并且因为本人极其的懒而懒得修复。而且如果我在后面的提交中修复了它们，对读者而言也很麻烦——还得切换到后面的lab的分支才能看到这些修改……
所以就有了这个文档。如果你单纯拷贝我的代码交上去，那么你将提交和我一样的BUG……

1.semant部分，OpExp的检查不够细致。Translation阶段并不提供字符串和结构体的大小比较，仅提供EQ和NEQ。
2.semant部分，BreakExp后不应当有其他无法执行的Exp。
3.semant部分，假设有一if-then-else语句，then子句break，则不应当视为有误(该死的break)。
4.translation部分，op_need_cond解决方案无法较好地解决使用if表示i1条件的问题，也没有处理一个i32和一个i1的比较。
5.lab5-part1的代码异常混乱，我严重怀疑其中还有不少没考虑到的情况，虽然它足以通过lab测试。
6.lab5-part2有一些过分tricky的处理，严重依赖tiger语言的feature。
7.lab5-part2的cmp条件翻译可能有漏洞。